module.exports = {
    // Specify the environment where your code will run
    env: {
      browser: true,
      node: true,
      es6: true,
    },
    // Use the Airbnb style guide rules
    extends: 'airbnb',
    // Define any additional rules or overrides
    rules: {
      // Example custom rule
      'my-custom-rule': 'warn',
    },
  };
  